{
  "name": "pwlogicon-platform",
  "version": "1.0.1",
  "main": "app.js",
  "scripts": {
    "start": "NODE_ENV=production node app.js",
    "build": "mkdir -p db && npm install",
    "render-build": "npm run build",
    "dev": "nodemon app.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.7",
    "geolib": "^3.3.3",
    "helmet": "^7.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^6.8.1",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": "18.x"
  }
}
